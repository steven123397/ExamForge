# Store this file outside the repository as a separate 600-permission file.
# Values must be the current account passwords, not bootstrap values from .env.
ONLINE_ADMIN_PASSWORD=<current-admin-password>
ONLINE_OPERATOR_PASSWORD=<current-operator-password>
ONLINE_TEACHER_PASSWORD=<current-teacher-password>
ONLINE_STUDENT_PASSWORD=<current-student-password>
