{
  "name": "@examforge/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --test-concurrency=1 --import tsx tests/*.test.ts"
  },
  "dependencies": {
    "@examforge/db": "0.1.0",
    "@examforge/scheduling-application": "0.1.0",
    "@examforge/shared": "0.1.0",
    "bullmq": "^5.80.2",
    "ioredis": "^5.11.1"
  }
}
