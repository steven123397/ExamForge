[project]
name = "examforge-scheduler"
version = "0.1.0"
description = "ExamForge scheduling prototype"
requires-python = ">=3.12"
dependencies = ["ortools>=9.10"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
